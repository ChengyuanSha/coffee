import CoffeeButton from './CoffeeButton'

const Body = () => {
  return (
    <body className="body">
        <CoffeeButton color='blue' text='coffee'/>
        <CoffeeButton color='blue' text='coffee'/>
        <CoffeeButton color='blue' text='coffee'/>
        <CoffeeButton color='blue' text='coffee'/>
    </body>
  )
}

export default Body